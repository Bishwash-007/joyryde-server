// Google Maps client dependency removed; stub retained for future integration.
// Replace this implementation with a fetch to Google Maps Geocoding API when providing an API key.

export async function geocodeAddress(_address) {
  // Stubbed response to avoid external dependency.
  return null;
}
